# 修改紀錄

本檔案記錄此原型的重要變更。版本編號採 [語意化版本](https://semver.org/lang/zh-TW/)。

## [0.8.0] - 2026-09-02

### 新增：實際寫入

改版前送出只是跳出一則訊息，沒有任何資料被儲存。現已可真正寫入 Google 試算表。

- **新增 `Assessments` 工作表，29 欄。** 涵蓋狀態、學員與教師身分、科別、三個時間點、醫療資訊、病例複雜程度、整體信賴授權、七面向各自的尺規、片語組合結果、質性回饋、來源。
- **學員送出申請寫入一列，教師送出評量更新該列。** 學員修改醫療資訊亦走更新路徑。
- **逐列的所有權檢查 `canWriteRow()`。** 原本的 `canWrite()` 只判斷能不能碰某張表，這一層判斷能不能碰某一列：
  - 學員只能改自己的申請，且僅限醫療資訊三欄與就診時間，不可觸碰任何評量內容
  - 學員在已評量後 7 日內才可修改，逾期後端拒絕
  - 教師只能評自己被指定的申請，且不可修改學員填寫的醫療資訊
  - 狀態為「未填寫」時，教師的評量寫入會被後端拒絕，與介面規則一致
  - 與自己無關的申請一律拒絕
- **新增申請時身分欄位由後端覆寫。** 表單編號、學員編號、學員姓名、申請時間皆以後端取得的登入身分為準，忽略前端送來的值，避免冒名申請。

### 變更

- **送出按鈕旁標示本次是否會寫入。** 展示版顯示灰色的「展示版，不會寫入」，正式版顯示綠色的「會寫入試算表」。成功訊息也據此區分，不再讓「示範」看起來像「已存檔」。
- 寫入失敗時顯示後端回傳的具體原因，例如逾期不可修改、醫療資訊未填完整、這筆申請與你無關。

### 說明

質性回饋只顯示已評分面向的建議片語一事，於 0.7.0 即已實作。第二步選為「未觀察」的面向，第三步不會出現。

---

## [0.7.1] - 2026-09-02

### 修正

- **修正「申請評量」與「學員申請通知」兩頁白屏。** 兩頁都用到翻譯鍵 `teacher`，但字典中只有 `teacherName`。`t()` 對缺鍵直接取 `dict[key][lang]`，`dict["teacher"]` 為 undefined，取屬性即拋出 TypeError，整頁渲染失敗。
- **`t()` 改為缺鍵時不崩潰。** 找不到鍵時回傳鍵名本身並在主控台留下警告，畫面仍可操作，也看得出是哪個鍵漏了。缺鍵仍是開發錯誤，但不該讓使用者看到白屏。

### 新增

- `check-i18n.py` 打包前檢查，比對所有 `t(lang, "...")` 呼叫與字典內容。此類缺鍵不會被 TypeScript 攔截，因為 `DictKey` 是從字典推導的字面量型別，而字串在部分位置經由樣板字串或 `as never` 繞過檢查，只會在執行期爆開。

---

## [0.7.0] - 2026-09-02

### 新增：評量系統

依據「mini-CEX.tw.entrust APP 操作手冊 教師版 2024-11-13 V4」建置。所有病歷號與病人資訊皆為虛構。

- **學員端「申請評量」。** 填寫指定教師、就診時間、科別與次專科、評估項目，以及三項醫療資訊。狀態依填寫完整度即時預告，送出前即可看到會是「待評量」還是「未填寫」。
- **狀態機四態。** 未填寫、待評量、已評量、教師取消。病歷號碼、地點、病人狀況或診斷任一未填完整即停留在「未填寫」，教師收得到但無法進入評量，只能提醒學員補齊。
- **教師端「學員申請通知」。** 依狀態分為待評量、未填寫、已評量三區，未填寫的項目會列出缺哪幾欄。
- **教師三步驟評量。**
  - STEP 1 信賴決策：病例複雜程度四級、學員整體表現五級，各自附上手冊中的判斷說明
  - STEP 2 七面向回饋：七個面向各附定義，尺規為未觀察、Below、Meet、Above
  - STEP 3 質性回饋：依 STEP 2 有評分的面向展開建議片語，嘉許與改善兩組，選取後自動組成回饋文字，另可自由補充
- **建議片語庫完整收錄。** 七面向合計 94 則，嘉許 47 則、改善 47 則。
- **回饋品質檢查。** 送出前套用 0.5.0 的四面向評估，屬教師自我檢查，不記錄也不呈現給學員。
- **病歷號遮蔽。** 僅於評量流程中對申請學員與指定教師顯示，管理平台與資料匯出一律遮蔽，與既有系統一致。

### 尚待確認

- 學員版操作手冊未取得，申請介面的必填規則與可修改範圍以教師版所載內容推定。
- 既有 APP 後端的介接方式未知，目前為獨立資料。雙向同步需 API 支援。
- 七面向尺規源自陳偉德教授 mini-CEX.tw，建議片語為教學部所編，正式使用前須確認授權範圍。
- 手冊第 22 頁起載有既有的「教學評估數據之管理平台」，功能與本平台高度重疊，兩者關係待釐清。
- 病歷號與床號屬醫療個資，原型階段一律填虛構值。正式資料存放位置須經院方資安確認。

---

## [0.6.0] - 2026-09-02

### 安全性修正

- **權限判斷全面移至後端。** 改版前身分由前端以網址參數傳遞（`?role=deptAdmin&staffId=...`），把 `role` 改成 `hospital` 即可取得全院資料，實際上等同沒有權限控制。現改由 `Session.getActiveUser().getEmail()` 取得登入者，回查 Users 表決定角色，前端傳來的任何身分參數一律忽略。
- **資料在回傳前裁切。** 改版前後端整包吐出十二張表，由前端決定顯示什麼，範圍外資料其實已送達使用者電腦，開啟開發者工具即可看見。現於後端完成裁切，科部承辦人收到的回應中就只有自己科的資料。
- **稽核紀錄僅系統管理者可讀取。** 其餘角色的回應中不含 AuditLogs 與 AuditChanges。
- **寫入權限由 `canWrite()` 在後端判斷。** 稽核表對所有角色皆不可寫。被拒絕的寫入同樣留下紀錄，記下是誰試圖做什麼。
- **名冊欄位依角色裁切。** 非系統管理者僅取得員工編號、姓名、職稱、科部，不含聯絡方式。

### 新增

- `ARCHITECTURE.md`，說明為何前端程式碼藏不住、以及正確的做法是什麼
- Apps Script 以 `HtmlService` 提供前端網頁，使前後端同源，得以取得 Google 登入身分
- `runtimeMode()` 自動判斷執行環境；靜態託管時左下角標示為展示版，明確告知權限僅為前端模擬
- 產出兩份建置：`index.html`（展示版，虛構資料）與 `apps-script/index.html`（正式版，貼入 Apps Script）

### 說明

前端程式碼無法隱藏，這是所有網頁應用的共同前提。此次改版的目標不是把程式碼藏起來，而是讓程式碼被看光或竄改都不影響資料安全，如此便能安心公開於 GitHub。

### 已知限制

- 正式版必須部署為「執行身分：存取應用程式的使用者」，此設定與展示版不同。
- Users 表可被有試算表編輯權的人修改，包括把自己的信箱改為 sysAdmin。試算表共用權限必須嚴格控制。
- Apps Script 仍取不到呼叫端 IP。
- Apps Script 有執行時間與呼叫次數配額，使用人數多時需評估遷移至正式後端。

---

## [0.5.1] - 2026-09-02

### 修正

- **修正科部儀表板的權限外洩。** 「各科表現比較」原本對所有角色顯示全院五個科部的平均與完成率，科部行政承辦人與科部教學推動人因此能看到自己負責範圍以外的科別資料。
  - `DeptDashboard` 原本完全沒有接收使用者資訊，無從判斷可見範圍。現已傳入 `user` 與 `role`
  - 科部角色的比較圖只保留自己負責的科部，以科部代碼前綴比對，含次專科
  - 科部角色只有單一科別，橫向比較沒有意義，因此直接呈現該科的次專科分布，並停用返回全院的按鈕
  - 圖表下方加上範圍說明，明確告知跨科比較僅全院型主管可見

### 已知限制

- 此過濾在前端進行，可被繞過。正式版必須在後端查詢層再做一次，前端過濾只能改善介面，不能當作權限控制。

---

## [0.5.0] - 2026-09-02

### 新增

- **質性回饋品質評估，四個面向。** 具體程度（有無舉出實際情境）、可執行性（學員是否知道下一步）、平衡性（嘉許與建議的比例）、與評分一致性（文字語氣是否與尺規分數相符），各給 0 至 100 分並附說明與改寫提示。
- **規則式評分為基底，AI 為深化層。** 四面向分數由文字特徵規則計算，不需連線即可運作，靜態展示版也能完整示範。設定 AI 代理後才提供逐則改寫與跨則習慣分析。
- **教師端「回饋撰寫助手」改版。** 草稿輸入拆為嘉許與建議兩欄，可選填七項尺規分數以啟用一致性判斷；四面向分數隨輸入即時更新；AI 深化會回傳改寫版本，可一鍵套回輸入框。
- **教師端新增「我的回饋品質回顧」。** 回顧先前送出的質性回饋，顯示平均品質、最需補強面向、各面向平均，並可逐則展開查看分數與理由。AI 可分析跨則的寫作習慣，指出反覆出現的強項與待改進處並給三條建議。
- **科部與全院儀表板新增「全科回饋品質」。** 呈現全科平均與各面向平均，低於 70 分的面向會列為建議提供教學支援的方向。

### 設計取捨

- **不呈現個別教師排名。** 回饋品質受病例情境與時間壓力影響，單一分數不足以評斷教學表現。此區塊的用途是找出需要教學支援的面向，介面上已明確標示。
- **一致性採容忍區間而非線性比對。** 期望的嘉許占比壓縮在 0.35 至 0.75，因為表現優異仍應有建議、表現不佳仍應有嘉許，落差 0.25 以內視為相符。
- **AI 改寫明確要求不得虛構臨床事實。** 提示詞規定只補強表達方式、保留教師原本的觀察，資訊不足時直接說明缺少什麼。介面上另有提醒請教師確認後再使用。

### 已知限制

- 規則式評分依賴中文關鍵詞比對，英文回饋的評分準確度較低。
- 空泛用語與具體線索詞為固定清單，不同科別的用語習慣可能需要調整。
- 分數僅為粗略指標，介面上已標示不能取代教學專業判斷。

---

## [0.4.0] - 2026-09-02

### 新增

- **完整稽核軌跡。** 稽核從單一扁平清單改為事件表加異動明細表的兩層結構，可回溯「誰、何時、對哪一筆、把哪個欄位從什麼改成什麼」。
  - `AuditLogs` 擴充為 17 欄，新增工作階段代碼、行為者角色、事件類別、對象型別與代碼、成功失敗、失敗原因、異動集代碼、User-Agent、來源
  - 新增 `AuditChanges` 表，每個被修改的欄位一列，記錄變更前後值，以 `change_set_id` 串回事件
  - 事件分四類：登入登出、檢視、修改、匯出下載，可於介面篩選
  - 修改類事件可展開查看欄位層級的變更明細，變更前的值以刪除線呈現
- **登入與登出留下紀錄。** 登入、登出、檢視科部與全院儀表板、檢視帳號管理、匯出清單、下載 PDF 皆會產生事件。
- **本次工作階段即時紀錄。** 稽核頁上方顯示當次登入的操作紀錄，格式與歷史紀錄相同，讓稽核行為本身可被驗證。
- **後端自動產生異動明細。** `Code.gs` 新增 `update` 操作，會先讀取原值、逐欄比對、只寫入真正變動的欄位，並自動產生對應的 `AuditChanges` 列。任何寫入都必然留下紀錄，前端無法略過。
- `logEvent()` 與 `writeChange()` 後端函式，以及流水號產生器 `nextId()`

### 變更

- 稽核表列入寫入白名單之外，前端不可直接新增或修改稽核紀錄
- 寫入失敗時也會留下一筆 `result=fail` 的事件並記錄失敗原因

### 已知限制

- **IP 位址無法由瀏覽器取得，Apps Script 亦未提供呼叫端 IP。** 原型中前端送出的 IP 一律記為「(未取得)」，示範資料中的位址為虛構。正式系統必須由後端從 HTTP 連線取得來源位址，且永不信任前端傳來的 IP。
- 裝置欄位由 User-Agent 推測，非硬體回報的真實機型。
- 行為者身分目前由前端傳入，可被偽造。接真實資料前必須改由 `Session.getActiveUser().getEmail()` 取得後回查 Users 表。
- 稽核紀錄目前可被有試算表編輯權的人直接修改。正式系統應寫入僅可新增的儲存體，並定期異地備份。

---

## [0.3.4] - 2026-09-02

### 變更

- 更新 `window.__SHEETS_API__` 為新的 Apps Script 部署網址。舊部署已停用。

---

## [0.3.3] - 2026-09-02

### 修正

- **修正獨立型 Apps Script 專案無法讀取試算表的問題。** `Code.gs` 原本只用 `SpreadsheetApp.getActiveSpreadsheet()` 取得試算表，該方法僅在腳本綁定於試算表時有效。若腳本是從 script.google.com 直接建立（獨立型），該方法回傳 null，呼叫端點會得到 `TypeError: Cannot read properties of null (reading 'getSheetByName')`。
  - 新增 `SPREADSHEET_ID` 設定項，獨立型腳本填入試算表 ID 即可運作
  - 新增 `getSpreadsheet()` 統一取得試算表，綁定型腳本維持留空自動判斷
  - 未設定 ID 且非綁定型時，改為拋出中文說明而非隱晦的型別錯誤

### 新增

- `testConnection()` 診斷函式，可在編輯器直接執行以確認試算表連線與工作表清單
- `setupSheets()` 在獨立型腳本執行時改寫入「執行紀錄」，不再因缺少試算表介面而失敗

---

## [0.3.2] - 2026-09-02

### 修正

- **修正 `<meta charset>` 位置導致設定腳本失效的問題。** 字元編碼宣告原本被排在檔案第 64941 個位元組處，遠超過 HTML 規範要求的前 1024 位元組。瀏覽器在讀到宣告前已用預設編碼解析檔案開頭，使得含中文註解的設定腳本無法正確執行，`window.__SHEETS_API__` 因此未生效，畫面右下角的資料來源指示器不會出現。已將 charset 與 viewport 移至 `<html>` 標籤之後的第一個位置。

---

## [0.3.1] - 2026-09-02

### 修正

- **啟用 Google Sheets 資料來源設定。** `window.__SHEETS_API__` 原本位於 `/* */` 註解區塊內，填入網址後不會生效，平台仍讀取內建示範資料。已將該行移出註解。

### 變更

- 設定區改用 `//` 行註解，避免整段被誤留在區塊註解中

---

## [0.3.0] - 2026-09-02

### 修正

- **修正儀表板點開學員明細時整頁當機的錯誤。** 「該學員評量明細」彈窗中誤植了資料匯出頁的 PDF 下載欄位，其中的 `isStudent` 與 `r` 變數在該元件並不存在，點擊執行學員的筆數會拋出 ReferenceError 造成白屏。此問題影響科部行政承辦人、科部教學推動人、全院型主管與系統管理者。

### 新增

- **Google Sheets 資料層。** 平台可改由 Google 試算表提供資料，未設定時仍使用內建示範資料。
  - `apps-script/Code.gs`：Apps Script 後端，提供讀取與有限度的寫入
  - `data/`：12 張工作表的欄位範本與虛構資料
  - 畫面右下角顯示目前資料來源與筆數，連線失敗自動退回內建資料

### 變更

- `index.html` 的設定區新增 `window.__SHEETS_API__`，與既有的 `window.__AI_PROXY__` 並列

---

## [0.2.0] - 2026-09-02

### 新增

- **AI 分析功能**，三處使用大型語言模型：
  - 儀表板洞察聚類：從質性回饋歸納常見好表現與待改進處
  - 學習建議：交叉比對各科成績與 mini-CEX 回饋
  - 回饋撰寫助手：檢查教師回饋草稿的具體程度並提供改寫
- **去識別化前置過濾。** 送往模型前自動遮蔽九類可識別資訊（身分證號、電子郵件、電話、病歷號、員工編號、床號、人名、完整日期、地址），過濾寫在 API 封裝層，任何呼叫都無法繞過。
- **教材待辦。** 儀表板洞察可建立待辦並指派給科部教學推動人，於內容擴增頁追蹤。
- **靜態託管的優雅降級。** 偵測到 GitHub Pages 等環境時，AI 按鈕自動停用並顯示說明，其餘功能不受影響。
- 授權條款（LICENSE）與專案說明（README）。

### 變更

- 「以此洞察修改網頁」改為「建立教材待辦」，行為與責任歸屬更明確
- 管理端「科別成績模組」更名為「科別配分設定」，與學員端「我的成績」區隔
- 所有人名、員工編號、信箱與機構名稱置換為虛構資料
- Landing page 加上設計原型聲明

---

## [0.1.0] - 2026-09-02

### 新增

- 平台初版，含六種角色的 RBAC 權限設計與 landing page
- 前台：個人評量（信賴授權矩陣、七構面雷達圖、學習進度、質性回饋、引導思考、學習歷程）、我的成績、學習資源
- 後台：科部與全院儀表板（次專科下鑽、逾期升級規則）、資料匯出、帳號管理、內容擴增
- 帳號管理：帳號列表、科部承辦人指派與交接、名單上傳、實習排程上傳、配分表上傳、批次更新、稽核軌跡
- 分角色的匯出權限：科部限本科含次專科、教師限自己評量過的、學員限自己被評量的並可下載單筆 PDF
- 中英雙語切換、咖啡廳風格視覺、響應式版面
